public class ProcessImage {

    // this initializes PCBs as a "process" starts -> begins by setting state as "NEW"

    public PCB Pcb_data;
    public String code;

    // add variables to help us latency and response

    // initializing process
    public void Process(String process)
    {
        // set PCB_data, code, and other variables
        // set state as "NEW"
    }

}
